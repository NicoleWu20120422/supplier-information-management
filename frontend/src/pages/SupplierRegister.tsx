import React, { useState } from "react";import { TextField, Button, Box } from "@mui/material";import supplierService from "../services/supplierService";const SupplierRegister: React.FC = () => {const [name, setName] = useState("");const [commodity, setCommodity] = useState("");const handleSubmit = async (e: React.FormEvent) => {e.preventDefault();await supplierService.register({ name, commodity });window.location.href = "/";};return (<Box component="form" onSubmit={handleSubmit} sx={{ m: 2 }}><TextField label="Supplier Name" value={name} onChange={e => setName(e.target.value)} required fullWidth sx={{ mb: 2 }} /><TextField label="Commodity" value={commodity} onChange={e => setCommodity(e.target.value)} required fullWidth sx={{ mb: 2 }} /><Button type="submit" variant="contained">Register</Button></Box>);};export default SupplierRegister;